pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building the application...'
                // Yahan apni build command likh sakte hain
            }
        }
        
        stage('Test') {
            steps {
                echo 'Running tests...'
                // Yahan apni test commands likh sakte hain
            }
        }
        
        stage('Deploy') {
            steps {
                echo 'Deploying application...'
                // Yahan apni deploy command likh sakte hain
            }
        }
    }
}
